[project]
name = "rattagatta-build"
version = "0.1.0"
description = "Build toolchain for rattagatta firmware"
requires-python = ">=3.10"
dependencies = [
    "platformio==6.1.19",
]

[tool.uv]
dev-dependencies = []
